#include <stdio.h>

/**
 * main - This program displays a single line of text using printf function.
 * Return: (0) if success, else code error.
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
